services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_DB: autodiagnostix
      POSTGRES_USER: autodiagnostix
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-devpassword}
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U autodiagnostix"]
      interval: 10s
      timeout: 5s
      retries: 5

  cms:
    build:
      context: ./cms
      dockerfile: Dockerfile
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      DATABASE_URL: postgres://autodiagnostix:${POSTGRES_PASSWORD:-devpassword}@postgres:5432/autodiagnostix
      PAYLOAD_SECRET: ${PAYLOAD_SECRET:-dev-secret-change-in-production}
      FRONTEND_URL: ${SITE_URL:-https://autodiagnostix.com}
    depends_on:
      postgres:
        condition: service_healthy
    volumes:
      - media:/app/public/media

  astro:
    build:
      context: ./astro
      dockerfile: Dockerfile
    restart: unless-stopped
    ports:
      - "4321:4321"
    environment:
      PAYLOAD_URL: http://cms:3000
      SITE_URL: ${SITE_URL:-https://autodiagnostix.com}
    depends_on:
      cms:
        condition: service_started

volumes:
  pgdata:
  media:
