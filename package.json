{
  "name": "site-design-antigravity",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "npm run dev --prefix app",
    "dev:cms": "npm run dev --prefix strapi",
    "build": "npm run build --prefix app",
    "build:cms": "npm run build --prefix strapi",
    "start:cms": "npm run start --prefix strapi",
    "lint": "npm run lint --prefix app",
    "preview": "npm run preview --prefix app",
    "seed": "npm run seed --prefix strapi",
    "docker:up": "docker compose up -d",
    "docker:down": "docker compose down"
  },
  "dependencies": {
    "react": "^19.2.4",
    "react-dom": "^19.2.4"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.4",
    "@types/react": "^19.2.14",
    "@types/react-dom": "^19.2.3",
    "@vitejs/plugin-react": "^6.0.1",
    "eslint": "^9.39.4",
    "eslint-plugin-react-hooks": "^7.0.1",
    "eslint-plugin-react-refresh": "^0.5.2",
    "globals": "^17.4.0",
    "vite": "^8.0.1"
  }
}
